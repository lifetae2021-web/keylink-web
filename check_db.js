import { initializeApp } from "firebase/app";
import { getFirestore, collection, query, where, getDocs } from "firebase/firestore";
import { readFile } from 'fs/promises';

async function run() {
  // Try to find the user '태영훈기본'
  const firebaseConfigPath = '/Users/lifetae2021/Desktop/keylink/src/lib/firebase.ts';
  // I will just use grep to find if there's any app Data
  console.log("Checking script...");
}
run();
